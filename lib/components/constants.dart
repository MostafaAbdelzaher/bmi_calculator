import 'dart:ui';

import 'package:flutter/material.dart';

const activeCardColor = Color(0xFF1D1E33);
const inActiveCardColor = Color(0xFF111328);
enum Gender { female, male }
const TextStyle labelText = TextStyle(fontSize: 18, color: Color(0xFF8D8E98));
const titleResultStyle =
    TextStyle(fontWeight: FontWeight.bold, fontSize: 40, color: Colors.white);
const bmiTextStyle =
    TextStyle(fontWeight: FontWeight.bold, fontSize: 100, color: Colors.white);
